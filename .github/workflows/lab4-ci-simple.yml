name: 🐳 Lab 4 - Simple CI Test

on:
  push:
    branches: [lab4-docker-compose]
  workflow_dispatch:

jobs:
  test-simple:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Show changes
      run: |
        echo "✅ CI workflow запущен!"
        echo "Branch: ${{ github.ref }}"
        echo "Event: ${{ github.event_name }}"
        ls -la docker-compose.yml Dockerfile
        echo "Файлы Lab 4 проверены успешно"
